apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: jekyll-virtualservice
  namespace: jekyll
spec:
  hosts:
  - montkim.com
  - blog.montkim.com
  gateways:
  - istio-system/montkim-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: jekyll-service
        port:
          number: 80
    timeout: 30s
    retries:
      attempts: 3
      perTryTimeout: 10s
