apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kumaentities.autokuma.bigboot.dev
spec:
  group: autokuma.bigboot.dev
  names:
    kind: KumaEntity
    plural: kumaentities
    singular: kumaentity
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for KumaEntitySpec via `CustomResource`
        properties:
          spec:
            properties:
              config:
                # additionalProperties: true는 중첩 객체(예: notification의 provider config)
                # 필드를 구조적 스키마 pruning으로 잘라낸다 — preserve로 대체
                x-kubernetes-preserve-unknown-fields: true
                type: object
            required:
            - config
            type: object
        required:
        - spec
        title: KumaEntity
        type: object
    served: true
    storage: true
