apiVersion: autokuma.bigboot.dev/v1
kind: KumaEntity
metadata:
  name: wedding-app-stage
  namespace: wedding-invitation
spec:
  config:
    type: http
    name: wedding-app (stage)
    url: https://we.makgol.com
    interval: 60
    max_retries: 3
