apiVersion: autokuma.bigboot.dev/v1
kind: KumaEntity
metadata:
  name: study-starlight-stage
  namespace: study-starlight
spec:
  config:
    type: http
    name: study-starlight (stage)
    url: https://study-starlight.makgol.com
    interval: 60
    max_retries: 3
